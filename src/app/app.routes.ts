import { Routes } from '@angular/router';
import { OrderBuilderComponent } from './pages/order-builder/order-builder.component';

export const routes: Routes = [{
    path: '', component: OrderBuilderComponent,
}];
